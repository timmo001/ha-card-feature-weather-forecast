---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@a64b87386f559bbf21c9d659266ce1ea23ead8f1 # master
    permissions:
      contents: write
